export interface GenderDisplay {
  icon: string;
  color: string;
}

export interface CheckboxStyle {
  base: string;
  unselected: string;
  selected: string;
  checkIcon: string;
  checkMark: string;
  checkMarkSelected: string;
}
